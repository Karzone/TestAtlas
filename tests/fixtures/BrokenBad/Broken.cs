namespace Broken.Bad
{
    // Never reaches the map — its project fails to load. Present only so the project is non-empty.
    public class NeverIndexed
    {
        public void Unreachable() { }
    }
}
